import { createContext } from 'react'

const noteContext = createContext('');

export default noteContext;
//Context API is used so that any state can be accesed by any component residing in the react app from everywhere